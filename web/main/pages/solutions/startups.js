import React from 'react';

import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles(() => {});

function StartupSolution() {
  return 'Startups';
}

export default StartupSolution;
