enum MediaType {
  Image
  Video
}

type Media @model {
  id: ID! @isUnique
  height: Int
  source: String!
  type: MediaType!

  profileMedia: Profile @relation(name: "ProfileImage")
  insightMedia: Insight @relation(name: "InsightMedia")
}