# import Query.*, Mutation.*, * from "comment.graphql"
# import Query.*, Mutation.*, * from "document.graphql"
# import Query.*, Mutation.*, * from "notification.graphql"
# import Query.*, Mutation.*, * from "order.graphql"
# import Query.*, Mutation.*, * from "person.graphql"
# import Query.*, Mutation.*, * from "product.graphql"

scalar Json

type Query {
  _empty: String # necessary for extends
}

type Mutation {
  _empty: String # necessary for extends
}