# import * from 'insights.graphql';
# import * from 'users.graphql';

type Query {
  insightsByCategory(category: String): [Insight!]!
}

type Mutation {
  createInsight: Insight
  createUser(name: String!): User
}
